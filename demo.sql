-- Get the first and last name of every player in the database.

SELECT first_name, last_name FROM players;

-- When starting SQL enable:
-- .mode
-- .headers on

-- .schema shows database schema
-- Run a file by entering .read "name of file"; in the terminal